/*package com.example.maximilienmarco.controller

import org.apache.catalina.User
import org.springframework.http.ResponseEntity
import org.springframework.web.bind.annotation.GetMapping
import org.springframework.web.bind.annotation.RequestMapping
import org.springframework.web.bind.annotation.RestController

@RestController
@RequestMapping("/api/users")
class UserController {

    @GetMapping
    fun getUsers(): ResponseEntity<List<User>> {
        val users = // Appel à un autre repository ou à une API externe pour récupérer la liste des utilisateurs
            return ResponseEntity.ok(users)
    }

    // Ajoutez d'autres endpoints pour gérer les opérations liées aux utilisateurs
}*/
